/** 
 * Defining the application roles.
 * @enum 
 */
export enum ROLES {
    BASIC = 'BASIC',
    CREATOR = 'CREATOR',
    ADMIN = 'ADMIN'
}


/** 
 * Defining the access level.
 * @enum 
 */
export enum ACCESS_LEVEL {
    DEVELOPER = 30,
    MAINTAINER = 40,
    OWNER = 50
}